#include<stdio.h>
#include<stdlib.h>

typedef struct plano {
  int a;
  int b;
  int c;
  int d;
}Plano;

int main(int argc, char **argv) {
  // Definindo as entradas
  int m, n, i, j;
  m = atoi(argv[1]);
  n = atoi(argv[2]);
  Plano p[m];
  for(i = 3; i < m; i++) {
    
  }
  return 0;
}
