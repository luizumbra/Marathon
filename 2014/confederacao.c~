#include<stdio.h>
#include<stdlib.h>

typedef struct plano {
  int a;
  int b;
  int c;
  int d;
}Plano;

int main(int argc, char **argv) {
  // Definindo as entradas
  int m, n, i, j;
  m = atoi(argv[1]);
  n = atoi(argv[2]);
  Plano p[m];
  for(i = 0; i < m; i++) {
    for(j = i + 3; j < i + 4)
  }
  return 0;
}
